## 检测预测界面

这是项目里的本地检测预测界面，适合加载检测权重做单张图片推理。

### 启动方式

macOS / Linux:

```bash
bash start_desktop_ui.sh
```

Windows:

```bat
start_desktop_ui.bat
```

### 说明

- 这个界面依赖检测模型权重
- 如果你当前主要使用 `data/类别名/图片` 的分类成品，优先使用 `CLASSIFY_UI.md` 对应的分类界面
